[build]
builder = "nixpacks"
buildCommand = "pip install -r 'CRM messenger/requirements.txt'"

[deploy]
startCommand = "cd 'CRM messenger' && python3 bot.py"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
